version: "1.0"

profile:
  base:
    name: empty

  inspections:
    - group: ALL
      enabled: false

    - inspection: ParametersPerMethod
      options:
        m_limit: 5
      enabled: true

    - inspection: ParametersPerConstructor
      options:
        m_limit: 5
      enabled: true