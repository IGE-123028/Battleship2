# IGE-123016 - Tiago Pires
version: "1.0"

profile:
  base:
    name: empty

  groups:
    - groupId: DisableAll
      groups:
        - "ALL"

  inspections:
    - group: DisableAll
      enabled: false

    - inspection: ParametersPerMethod
      options:
        m_limit: 5
      enabled: true

    - inspection: ParametersPerConstructor
      options:
        m_limit: 5
      enabled: true