# IGE-123016 - Tiago Pires
name: "Qodana - Long Parameter List"

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  qodana:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      checks: write

    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: 'Qodana Scan - Long Parameter List'
        uses: JetBrains/qodana-action@v2024.3
        with:
          args: --profile-path,.qodana/profiles/long-parameter-list.yaml --fail-threshold 0
          pr-mode: false
        env:
          QODANA_TOKEN: ${{ secrets.QODANA_TOKEN_1695558519 }}
          QODANA_ENDPOINT: 'https://qodana.cloud'