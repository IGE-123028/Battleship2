# IGE-123028
name: "Qodana"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  qodana:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Run Qodana Analysis
        uses: JetBrains/qodana-action@v2026.1
        with:
          args: --config,./qodana.yaml --property=project.open.type=Maven
        env:
          QODANA_TOKEN: ${{ secrets.QODANA_TOKEN }}
